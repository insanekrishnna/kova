<!-- views/home.ejs -->


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kova | Discover Hidden Wonders</title>

    <!-- Google Fonts: 
         'Poppins' for clean UI text.
         'Playfair Display' for headlines to add that premium, human editorial feel. 
    -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-black: #1a1a1a;
            --text-gray: #555555;
            --light-gray-bg: #f9f9f9;
            --white: #ffffff;
            --card-radius: 24px; /* Soft, organic corners */
            --transition-speed: 0.5s;
        }

        body {
            font-family: 'Poppins',         sans-serif;
            background-color: var(--white);
            color: var(--primary-black);
            overflow-x: hidden;
        }

        /* --- Typography: The Human Element --- */
        h1, h2, h3, h4 {
            font-family: 'Playfair Display', serif;
            color: var(--primary-black);
            font-weight: 700;
        }

        /* --- Hero Section --- */
        .hero-section {
            position: relative;
            padding: 100px 0 120px 0;
            /* Subtle World Map Texture */
            background-image: url('https://upload.wikimedia.org/wikipedia/commons/8/80/World_map_-_low_resolution.svg');
            background-repeat: no-repeat;
            background-position: center;
            background-size: cover;
            /* Heavy overlay to keep it minimal and high-contrast */
            background-color: rgba(255, 255, 255, 0.95);
            background-blend-mode: overlay;
        }

        .hero-title {
            font-size: 3.8rem;
            line-height: 1.15;
            margin-bottom: 1.5rem;
            letter-spacing: -0.02em;
        }

        .hero-desc {
            font-size: 1.1rem;
            font-weight: 300;
            color: var(--text-gray);
            line-height: 1.8;
            margin-bottom: 3rem;
            max-width: 90%;
        }

        /* Button: Minimalist & Solid */
        .btn-explore {
            background-color: var(--primary-black);
            color: var(--white);
            padding: 18px 38px;
            font-size: 1rem;
            font-weight: 500;
            border-radius: 50px;
            border: 1px solid var(--primary-black);
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }

        .btn-explore:hover {
            background-color: transparent;
            color: var(--primary-black);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        /* --- Hero Image Layout --- */
        .image-card {
            border-radius: var(--card-radius);
            overflow: hidden;
            position: relative;
            background-color: #eee;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
            transition: transform var(--transition-speed) ease;
        }

        .image-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            /* EMOTIONAL TOUCH: Start B&W, Bloom to Color on Hover */
            filter: grayscale(100%);
            transition: filter 0.6s ease, transform 0.8s ease;
        }

        /* Hover Effect: The "Discovery" Moment */
        .image-card:hover {
            transform: scale(1.02);
            z-index: 10;
        }

        .image-card:hover img {
            filter: grayscale(0%);
            transform: scale(1.08);
        }

        /* Grid Heights */
        .card-tall {
            height: 460px;
        }
        .card-short {
            height: 220px;
        }

        /* --- Feature Highlights --- */
        .features-section {
            padding: 80px 0;
            background-color: var(--white);
            border-top: 1px solid #f2f2f2;
        }

        .feature-box {
            padding: 30px 20px;
            text-align: center;
        }

        .icon-wrapper {
            width: 65px;
            height: 65px;
            /* Outline style for elegance */
            border: 1.5px solid var(--primary-black);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px auto;
            font-size: 1.4rem;
            color: var(--primary-black);
            transition: all 0.3s ease;
        }

        .feature-box:hover .icon-wrapper {
            background-color: var(--primary-black);
            color: var(--white);
            transform: rotateY(180deg);
        }

        .feature-box:hover .icon-wrapper i {
            transform: rotateY(-180deg); /* Keep icon facing forward */
        }

        .feature-title {
            font-size: 1.2rem;
            margin-bottom: 12px;
        }

        .feature-text {
            color: var(--text-gray);
            font-size: 0.95rem;
            font-weight: 300;
            line-height: 1.6;
        }

        /* --- Responsive Adjustments --- */
        @media (max-width: 991px) {
            .hero-section {
                text-align: center;
                padding-top: 60px;
            }
            .hero-title {
                font-size: 2.8rem;
            }
            .hero-desc {
                margin: 0 auto 2.5rem auto;
            }
            .hero-gallery {
                margin-top: 50px;
            }
            /* Stack Logic */
            .card-tall { height: 350px; }
            .card-short { height: 165px; }
        }

        @media (max-width: 576px) {
            .hero-title { font-size: 2.2rem; }
            .card-tall, .card-short { height: 250px; }
            .hero-gallery .col-6 { width: 100%; } /* Stack completely on mobile */
        }
    </style>
</head>

<body>

    <!-- 1. Navbar Partial -->
      <%- include("includes/navbar.ejs") %>


    <!-- 2. Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                
                <!-- Left Column: Copywriting -->
                <div class="col-lg-5 col-md-12">
                    <h1 class="hero-title">Discover the <br>World’s Hidden <br>Wonders</h1>
                    <p class="hero-desc">
                        Travel isn't just about the destination; it's about the feeling of arriving. 
                        Uncover homes with character, hosts with stories, and moments that become memories. 
                        Simple, authentic, and yours to explore.
                    </p>
                    <a href="/listings" class="btn btn-explore">
                        Explore Listings
                    </a>
                </div>

                <!-- Right Column: B&W to Color Grid -->
                <div class="col-lg-7 col-md-12 hero-gallery">
                    <div class="row g-4"> <!-- g-4 for airy spacing -->
                        
                        <!-- Large Card (Left) -->
                        <div class="col-lg-6 col-6">
                            <div class="image-card card-tall">
                                <img src="https://images.unsplash.com/photo-1470770841072-f978cf4d019e?q=80&w=800&auto=format&fit=crop" alt="Serene Cabin Landscape">
                            </div>
                        </div>

                        <!-- Stacked Cards (Right) -->
                        <div class="col-lg-6 col-6 d-flex flex-column gap-4">
                            <!-- Top -->
                            <div class="image-card card-short">
                                <img src="https://images.unsplash.com/photo-1493663284031-b7e3aefcae8e?q=80&w=800&auto=format&fit=crop" alt="Modern Living Room">
                            </div>
                            <!-- Bottom -->
                            <div class="image-card card-short">
                                <img src="https://images.unsplash.com/photo-1510798831971-661eb04b3739?q=80&w=800&auto=format&fit=crop" alt="Mountain Adventure">
                            </div>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </section>

    <!-- 3. Feature Highlights Section -->
    <section class="features-section">
        <div class="container">
            <div class="row justify-content-center">
                
                <!-- Feature 1 -->
                <div class="col-md-4 col-sm-6">
                    <div class="feature-box">
                        <div class="icon-wrapper">
                            <i class="fa-regular fa-compass"></i>
                        </div>
                        <h4 class="feature-title">Curated Stays</h4>
                        <p class="feature-text">We select homes that offer more than a bed—they offer an experience.</p>
                    </div>
                </div>

                <!-- Feature 2 -->
                <div class="col-md-4 col-sm-6">
                    <div class="feature-box">
                        <div class="icon-wrapper">
                            <i class="fa-regular fa-heart"></i>
                        </div>
                        <h4 class="feature-title">Trustworthy Reviews</h4>
                        <p class="feature-text">Real stories from real travelers. No bots, just honest feedback.</p>
                    </div>
                </div>

                <!-- Feature 3 -->
                <div class="col-md-4 col-sm-6">
                    <div class="feature-box">
                        <div class="icon-wrapper">
                            <i class="fa-regular fa-id-badge"></i>
                        </div>
                        <h4 class="feature-title">Verified Hosts</h4>
                        <p class="feature-text">Connect with hosts we've vetted personally for your peace of mind.</p>
                    </div>
                </div>

            </div>
        </div>
    </section>




    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>